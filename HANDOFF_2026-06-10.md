# HANDOFF 2026-06-10: Phase C 診断完了 → 次はライン特徴量

統合管理セッション (Public-Race-Management-System) からの作業引き継ぎ。
このドキュメントだけで次セッションが作業を開始できるように書いてある。

## TL;DR

- **Phase A 完了**: 最遅約定可能スナップショット = **発走1分前** で確定
- **Phase C 判定: 現行ベースラインは NO-GO** (EV選別の実現ROI 62-64%)
- 敗因を3段階で切り分け済み: オッズずれ(×) → 確率の歪み単体(×) → **モデルが市場に情報劣位(○)**
- 安い対策 (閾値調整 / offset変更 / isotonic校正) は**全て試して効かないことを実証済み**
- **次の一手はライン特徴量** (race_lines.csv は収集済み・モデル未投入)

## 1. 確定した事実 (再調査不要)

| # | 検証 | 結果 | 証跡 |
|---|---|---|---|
| 1 | 約定実現性 (snapshot→確定オッズ乖離) | 5分前は当選側が最大6.7%下振れ・下振れ率30-44%。**1分前は乖離中央値0.0%・下振れ率<3%** | reports/phase_ac_2026-06-10.md §Phase A |
| 2 | EV選別の実現ROI (offset 2/3/5分, thr 1.0-2.0) | **62-79%、全セルCI上限<105%、FDR(q=0.10)通過ゼロ** | 同 §Phase C |
| 3 | p_combo のキャリブレーション | 穴目(<0.01)を1.8-2.4倍過小評価 / 本命側(>0.1)を0.75-0.87倍過大評価 (Harville の既知欠陥) | 同 追補 |
| 4 | isotonic 校正後の再評価 (前半7日fit→後半8日eval) | **ROI 50-70%で改善なし** → 周辺校正では直らない | 同 追補 |

結論: EV>=1 の選別が拾っているのは「市場の歪み」ではなく「モデルの誤差」。
市場が織り込む情報 (ライン力学・直近フォーム・バンク相性) をモデルが持っていない。

## 2. 次タスク (優先順)

### Task 1: ライン特徴量の実装と再学習 (本丸)

素材は収集済み: `D:\keirin-ai-data\race_lines.csv`
(columns: race_date, place_code, race_no, car_no, **narabi_x** =ライン番号, **narabi_y** =ライン内位置)

`race_stats.csv` と結合して作れる特徴量の候補:
- ライン内位置 (narabi_y: 先頭=1 / 番手=2 / 3番手) と単騎フラグ (ライン人数=1)
- ライン人数・レース内ライン本数
- **ライン強度**: ライン構成員の heikin_tokuten (競走得点) の合計/平均/先頭の得点
- ライン先頭の脚質 (kyakusitu) × 自分の位置の交互作用 (逃げ先頭の番手は鉄板等)
- 自ラインと最強他ラインの得点差
- バンク特性: place_code 別の決まり手分布 (race_results.csv の kimarite から事前集計)

実装場所: ml_baseline.py の特徴量生成部 (Phase B で ml_pred_probs.csv を出している系)。
学習データは normal venue-days 13,544日分 (race_quality.csv で status='normal' フィルタ)。

### Task 2: ml_picks / ml_pred_probs の日次生成を再開 (運用)

2026-06-05 で停止中 (ml_baseline_phaseB.log が 6/6 06:44 の手動実行で最後)。
再判定には「新モデルの確率 × 1分前オッズ」の重なりが2-3週間必要なので、
モデル改良と並行して日次タスク化しておく (cron_*.bat 方式、**bat は cp932+CRLF・
日本語コメントを入れるなら chcp 65001 を冒頭に** — 2026-06-10 に banei/hokkaido/local が
UTF-8/LF bat でタスクスケジューラ 0xFF 即死した実例あり)。

### Task 3: 市場ブレンド (Task 1 の後の併用候補)

p' = w·p_model + (1-w)·p_market (p_market = (1/odds) をレース内正規化、overround除去)。
モデルが市場と乖離する場面だけに絞る定石。単体では bets が消えるので Task 1 とセット。

### Task 4: 再判定 (道具は完成済み)

```
python phase_ac_analysis.py
```
を再実行するだけ (reports/phase_ac_YYYY-MM-DD.md が出る)。
判定基準: 日次ブートストラップ CI + BH-FDR(q=0.10) 通過セルの有無。
**EV計算は必ず 1分前オッズ (target_offset_min=1) を基準にする** (Phase A の結論)。

## 3. データ資産の現在地 (D:\keirin-ai-data)

| ファイル | 内容 | 注意 |
|---|---|---|
| race_odds_prerace.csv (101MB) | 発走前snapshot。**offset 5/3/2分=2026-05-16〜、1/0.5分=2026-06-06〜** | 5/13-22はJSJ048問題で欠損 |
| ev_prerace_merged.csv | p_combo×オッズ×EV (Phase B 産物)。**2026-06-05 で停止中** | Task 2 で更新再開 |
| ml_pred_probs.csv | 車番単位 p_win/p_top2/p_top3 | 同上 |
| payouts.csv | 確定払戻。**payout/100 = 確定オッズ** (Phase A はこれと突合した) | |
| race_odds.csv (2.6GB) | 発走後0-5分の近似。**本番EV評価に使用禁止** (リーケージ) | 2026-05-12 更新停止(意図的) |
| race_lines.csv | ライン構成 (narabi_x/narabi_y)。**未活用 — Task 1 の主役** | |

総量 3.2GB / 2021-04〜の約5.7年分。
prerace daemon は毎日 8:00 に自動起動中 (1分前データは毎日貯まり続けている)。

## 4. してはいけないこと (今日実証済みの行き止まり)

- EV閾値を上げる (1.0→2.0 でROIフラット、改善しない)
- offset を変える (2/3/5分どれも同水準)
- isotonic 等の単調校正だけで本番に出す (ROI 改善せず)
- race_odds.csv での EV 評価 (発走後オッズ=リーケージ)

## 5. 関連ドキュメント

- reports/phase_ac_2026-06-10.md — Phase A/C 全数値と判定 (本ドキュメントの根拠)
- phase_ac_analysis.py — 再判定ツール (読み取り専用、DATA_DIR 環境変数対応)
- CLAUDE.md — Phase A/B/C の定義 (行79-139)、運用フロー
- 統合監視: 管理PJ (Public-Race-Management-System) が毎朝7:30に本PJの
  タスク成否・データ鮮度を監視中。ダッシュボード http://localhost:8521
